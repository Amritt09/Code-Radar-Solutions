#include<stdio.h>
int main(){
    int i,num;
    scanf("%d",&num);
   // for (int i = 2; i*i<=num ;i++){
        if(num %2 == 0)
        {
            printf("Prime");
        }
        else
        {
            printf("Not Prime");
        }
        return 0;
    }

   
