#include<stdio.h>
int main(){
    int i,num;
    scanf("%d",&num);
        if(num %2 == 0)
        {
            printf("Prime");
        }
        else

        {  if(num==1)
        printf("Not Prime");
        else {printf("Not Prime");}
           
        }
        return 0;
    }

   
